const nota1 = 8;
const nota2 = 7;
const nota3 = 6;

const media = (nota1 + nota2 + nota3) / 3;

const situacao = media >= 7 ? "Aprovado" : "Reprovado";

console.log(` Sua média é ${media} | Status: ${situacao}`);

